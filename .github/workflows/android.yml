name: Eden Android Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # Configura o runner e já aceita automaticamente todas as licenças do SDK/NDK
      - uses: android-actions/setup-android@v3

      - name: Grant permissions
        working-directory: ./src/android
        run: chmod +x gradlew || true

      # O segredo está na flag auto-download que força o Gradle a baixar o NDK correto sozinho
      - name: Build APK
        working-directory: ./src/android
        run: ./gradlew assembleRelease -Pandroid.injected.build.abi=arm64-v8a --android.download.missing.dependencies=true
